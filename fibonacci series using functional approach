Write a program to generate fabonachi series using functional approach.
Sol:
def incre(y):
  if y>num:
    print ("Fibonacci series finished")
  else:
    print(fib(y))
    return (incre(y+1))

def fib(x):
  
  if (x <= 1):
    return x
  else:
    return (fib(x-1)+fib(x-2))

num=int(input("Enter the nth position till you want fibonacci series:"))
incre(0)
